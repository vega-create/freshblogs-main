---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import CrossSiteLinks from '../../components/CrossSiteLinks.astro';
import { siteConfig } from '../../../site.config';

// Map site slugs to content collections
const collectionMap: Record<string, string> = {
  astrology: 'astrology-blog',
};

export async function getStaticPaths() {
  return siteConfig.sites.map((site) => ({
    params: { site: site.slug },
    props: { site },
  }));
}

const { site } = Astro.props;
const siteAuthor = siteConfig.authors.find((a) => a.sites.includes(site.slug));

// Load articles if collection exists
const collectionName = collectionMap[site.slug];
let articles: any[] = [];
if (collectionName) {
  try {
    const allPosts = await getCollection(collectionName as any);
    articles = allPosts
      .sort((a: any, b: any) => b.data.publishDate.getTime() - a.data.publishDate.getTime())
      .slice(0, 6);
  } catch {}
}
const formatDate = (d: Date) => d.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });

const title = `${site.name} — Free Tools, Quizzes & Expert Guides`;
const description = site.description + '. Free interactive tools, daily content, and expert articles. No sign-up required.';

const breadcrumbLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: siteConfig.url },
    { '@type': 'ListItem', position: 2, name: site.name, item: `${siteConfig.url}/${site.slug}/` },
  ],
};

// Tool lists per site — only tools with actual built pages
const toolsBySlug: Record<string, {name: string; emoji: string; path: string; desc: string}[]> = {
  astrology: [
    { name: "What's My Sign?", emoji: '🔮', path: 'zodiac-sign', desc: 'Enter your birthday to discover your zodiac sign' },
    { name: 'Zodiac Compatibility', emoji: '💕', path: 'compatibility', desc: 'Check love, friendship & work compatibility' },
    { name: 'Rising Sign Calculator', emoji: '⬆️', path: 'rising-sign', desc: 'Calculate your ascendant sign' },
    { name: "Today's Lucky Color", emoji: '🎨', path: 'daily-color', desc: 'Your personalized lucky color for today' },
    { name: 'Moon Sign Calculator', emoji: '🌙', path: 'moon-sign', desc: 'Discover your emotional moon sign' },
    { name: 'Birth Chart', emoji: '📊', path: 'birth-chart', desc: 'Simple birth chart visualization' },
    { name: 'Element Quiz', emoji: '🔥', path: 'element-quiz', desc: 'Are you Fire, Earth, Air, or Water?' },
    { name: 'Lucky Number', emoji: '🔢', path: 'lucky-number', desc: "Today's lucky numbers for your sign" },
    { name: 'Celebrity Match', emoji: '⭐', path: 'celebrity-match', desc: 'Which celebrity shares your sign?' },
    { name: 'Zodiac Trivia', emoji: '🎮', path: 'trivia', desc: 'Test your astrology knowledge' },
  ],
  tarot: [
    { name: 'Daily Card Pull', emoji: '🃏', path: 'daily-pull', desc: 'Your tarot card of the day' },
    { name: 'Three-Card Spread', emoji: '🔮', path: 'three-card', desc: 'Past, present, future reading' },
    { name: 'Celtic Cross', emoji: '✝️', path: 'celtic-cross', desc: 'Full 10-card spread reading' },
    { name: 'Yes or No Tarot', emoji: '❓', path: 'yes-no', desc: 'Quick yes/no answer from the cards' },
    { name: 'Love Tarot', emoji: '❤️', path: 'love-tarot', desc: '5-card love and relationship reading' },
    { name: 'Card Encyclopedia', emoji: '📖', path: 'encyclopedia', desc: 'Browse all 78 tarot cards' },
  ],
  numerology: [
    { name: 'Life Path Number', emoji: '🔢', path: 'life-path', desc: 'Calculate your life path number' },
    { name: 'Name Number', emoji: '✨', path: 'name-number', desc: 'Discover your name number meaning' },
    { name: 'Angel Numbers', emoji: '👼', path: 'angel-number', desc: 'Look up angel number meanings' },
  ],
  dreams: [
    { name: 'Dream Dictionary', emoji: '🌙', path: 'dictionary', desc: 'Look up dream symbol meanings' },
    { name: 'Dream Journal', emoji: '📓', path: 'journal', desc: 'Record and track your dreams' },
    { name: 'Dream Interpreter', emoji: '🔮', path: 'interpreter', desc: 'Describe your dream, get analysis' },
  ],
  personality: [
    { name: 'MBTI Personality Quiz', emoji: '🧠', path: 'mbti', desc: 'Discover your Myers-Briggs type' },
    { name: 'Love Language Quiz', emoji: '❤️', path: 'love-language', desc: 'Find your primary love language' },
    { name: 'Enneagram Test', emoji: '🔢', path: 'enneagram', desc: 'Discover your Enneagram type (1-9)' },
  ],
  manifest: [
    { name: 'Affirmation Generator', emoji: '💕', path: 'affirmations', desc: 'Positive affirmations by category' },
    { name: 'Gratitude Journal', emoji: '🙏', path: 'gratitude', desc: 'Daily gratitude practice with streaks' },
    { name: 'Vision Board', emoji: '🎯', path: 'vision-board', desc: 'Create your digital vision board' },
  ],
  quotes: [
    { name: 'Quote Generator', emoji: '💬', path: 'generator', desc: 'Inspiring quotes by category + daily quote' },
    { name: 'Quote Image Maker', emoji: '🖼️', path: 'image-maker', desc: 'Create shareable quote images' },
    { name: 'Quote Search', emoji: '🔍', path: 'search', desc: 'Search quotes by keyword or topic' },
  ],
  pets: [
    { name: 'Pet Match Quiz', emoji: '🐾', path: 'pet-match', desc: 'Find your perfect pet match' },
    { name: 'Pet Name Generator', emoji: '🏷️', path: 'name-generator', desc: 'Find the perfect name for your pet' },
    { name: 'Breed Info Guide', emoji: '📋', path: 'breed-guide', desc: 'Browse popular dog and cat breeds' },
  ],
  recipes: [
    { name: 'Kitchen Unit Converter', emoji: '⚖️', path: 'converter', desc: 'Convert cups, ml, grams, oz & temps' },
    { name: 'Cooking Timer', emoji: '⏱️', path: 'timer', desc: 'Multiple preset timers for cooking' },
    { name: 'Dinner Spinner', emoji: '🎰', path: 'spinner', desc: 'Random meal suggestion wheel' },
  ],
  ai: [
    { name: 'AI Prompt Generator', emoji: '🤖', path: 'prompt-generator', desc: 'Create effective prompts for AI tools' },
    { name: 'Word & Character Counter', emoji: '📝', path: 'word-counter', desc: 'Count words and check platform limits' },
    { name: 'AI vs Human Quiz', emoji: '🤖', path: 'ai-vs-human', desc: 'Can you spot AI-written text?' },
  ],
  travel: [
    { name: 'Packing Checklist', emoji: '🧳', path: 'packing', desc: 'Customized packing lists by trip type' },
    { name: 'Trip Budget Calculator', emoji: '💰', path: 'budget', desc: 'Estimate travel costs by destination' },
    { name: 'Destination Quiz', emoji: '🌍', path: 'destination-quiz', desc: 'Find your perfect destination' },
  ],
  diy: [
    { name: 'Paint Calculator', emoji: '🎨', path: 'paint-calculator', desc: 'Calculate paint needed for any room' },
    { name: 'Measurement Converter', emoji: '📏', path: 'measurement', desc: 'Convert length, area & volume units' },
    { name: 'Project Cost Estimator', emoji: '💰', path: 'cost-estimator', desc: 'Estimate home improvement costs' },
  ],
  garden: [
    { name: 'Plant Finder Quiz', emoji: '🌿', path: 'plant-quiz', desc: 'Find the right plant for your space' },
    { name: 'Watering Schedule', emoji: '🌱', path: 'watering', desc: 'Personalized plant watering schedule' },
    { name: 'Planting Calendar', emoji: '📅', path: 'planting-calendar', desc: 'When to plant in your zone' },
  ],
};

const tools = toolsBySlug[site.slug] || [];
---

<BaseLayout title={title} description={description} jsonLd={breadcrumbLd}>
  <div class="max-w-6xl mx-auto px-4 py-12">
    <!-- Breadcrumb -->
    <nav class="text-sm text-gray-400 mb-8">
      <a href="/" class="hover:text-white">Home</a>
      <span class="mx-2">/</span>
      <span class="text-white">{site.name}</span>
    </nav>

    <!-- Hero -->
    <div class="text-center mb-12">
      <span class="text-5xl mb-4 block">{site.emoji}</span>
      <h1 class="text-3xl md:text-4xl font-display font-bold mb-3">{site.name}</h1>
      <p class="text-lg text-gray-300 max-w-xl mx-auto">{site.description}</p>
      {siteAuthor && (
        <p class="text-sm text-gray-500 mt-2">
          by <a href={`/about/${siteAuthor.slug}/`} class="text-primary hover:underline">{siteAuthor.name}</a>
        </p>
      )}
    </div>

    <!-- Tools Grid -->
    {tools.length > 0 && (
      <section class="mb-12">
        <h2 class="text-xl font-bold mb-4">🛠️ Free Interactive Tools</h2>
        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-3">
          {tools.map((tool) => (
            <a
              href={`/${site.slug}/tools/${tool.path}/`}
              class="group flex items-center gap-3 p-4 bg-white/5 hover:bg-white/10 border border-white/10 rounded-xl transition-all duration-300"
            >
              <span class="text-2xl">{tool.emoji}</span>
              <div>
                <h3 class="font-semibold text-white group-hover:text-primary transition-colors text-sm">
                  {tool.name}
                </h3>
                <p class="text-xs text-gray-400">{tool.desc}</p>
              </div>
            </a>
          ))}
        </div>
      </section>
    )}

    <!-- Blog Articles -->
    <section class="mb-12">
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-xl font-bold">📝 Latest Articles</h2>
        {articles.length > 0 && (
          <a href={`/${site.slug}/blog/`} class="text-sm text-purple-400 hover:text-white transition-colors">
            View all →
          </a>
        )}
      </div>
      {articles.length > 0 ? (
        <div class="grid gap-3">
          {articles.map((post: any) => (
            <a
              href={`/${site.slug}/blog/${post.id}/`}
              class="group flex justify-between items-center gap-4 p-4 bg-white/5 hover:bg-white/10 border border-white/10 rounded-xl transition-colors"
            >
              <div>
                <h3 class="font-semibold text-white group-hover:text-purple-400 transition-colors text-sm">
                  {post.data.title}
                </h3>
                <p class="text-xs text-gray-500 mt-1 line-clamp-1">{post.data.description}</p>
              </div>
              <time class="text-xs text-gray-500 whitespace-nowrap flex-shrink-0">
                {formatDate(post.data.publishDate)}
              </time>
            </a>
          ))}
        </div>
      ) : (
        <p class="text-gray-400">New articles coming soon! Check back for expert guides and insights about {site.name.toLowerCase()}.</p>
      )}
    </section>

    <!-- Cross-site links -->
    <CrossSiteLinks currentSite={site.slug} />
  </div>
</BaseLayout>
